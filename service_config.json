{"methodConfig": 
    [{
     "name": [{"service": "lock_service.LockService", "method": "client_init"}],
     "timeout":"2.0s",
     "retryPolicy": 
                    {"maxAttempts": 5, 
                    "initialBackoff": "0.1s", 
                    "maxBackoff": "1s", 
                    "backoffMultiplier": 2, 
                    "retryableStatusCodes": ["UNAVAILABLE","DEADLINE_EXCEEDED"]}
                    
    }
    ]
}